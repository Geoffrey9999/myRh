<% @instances.each do |instance| %>
    <a href="<%= url_for(controller:'instances', action:'edit', id: instance.id) %>"><%= instance.date + "  " + instance.companie.company_name + " " + instance.companie.company_location %></a>
    <br />
<% end %>