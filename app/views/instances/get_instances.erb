<% @instances.each do |instance| %>
    <a href="<%= url_for(controller:'instances', action:'edit', id: instance.id) %>"><%= instance.date + instance.company + instance.town %></a>
    <br />
<% end %>