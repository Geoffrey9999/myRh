
<form class="form-horizontal" action="/instances/<%= @id.id %>" accept-charset="UTF-8" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <input name="utf8" value="✓" type="hidden">
  <input name="_method" value="patch" type="hidden">

  <% @get_instance.each do |f| %>

      <label for="date"><%= f.date %></label>
      <input type="date" name="date" id="date" placeholder="<%= f.date %>">

      <label for="company"><%= f.company %></label>
      <select id="company"  name="company">
        <option disabled selected>Choisiez une entreprise</option>
        <% @company.each do |x| %>

            <option value="<%= x.company_name %>"><%= x.company_name + "  -  " + x.company_location %></option>
        <% end %>
      </select>

      <br />
      <br />
      <input type="submit" value="modifier">
  <% end %>
</form>


